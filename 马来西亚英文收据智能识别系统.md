# 马来西亚英文收据智能识别与管理系统

## 产品需求文档 V1.2

| 文档信息 | 内容 |
| --- | --- |
| 项目名称 | 马来西亚英文收据智能识别与管理系统 |
| 当前版本 | V1.2 |
| 架构决策 | 静态前端 + Supabase + Supabase Edge Functions |
| 目标用户 | 马来西亚中小企业财务人员、个体经营者 |
| 部署目标 | 公网可访问，无自建服务器 |

## 1. 项目目标

构建一个 Web 应用，实现：

```text
上传收据
  -> OCR 文字识别
  -> AI 信息抽取
  -> 自动分类
  -> 标签管理
  -> 云端列表存储
  -> 人工校对
  -> Excel 导出
```

系统不部署传统后端服务器。前端静态托管，数据与文件存储使用 Supabase，OCR/AI 密钥通过 Supabase Edge Function 保护。

## 2. 用户角色

| 角色 | 核心需求 |
| --- | --- |
| 财务操作员 | 批量上传、自动识别、快速校对、导出 Excel |
| 财务主管 | 按状态、类型、标签筛选，查看历史记录 |
| 个体经营者 | 低成本、少配置、浏览器即可使用 |

## 3. 功能需求

### 3.1 登录与数据隔离

- 用户通过 Supabase Auth 登录。
- 每条收据绑定 `user_id`。
- 用户只能查看、修改、删除自己的收据。
- Supabase 表和 Storage 必须开启 RLS/Policy。

### 3.2 文件上传

- 支持 PDF、JPEG、PNG。
- 单次最多 20 个文件。
- 单文件建议限制 20MB。
- 文件上传至 Supabase Storage 私有 bucket：`receipts`。
- 推荐路径：`receipts/{user_id}/{receipt_id}/original.ext`。

### 3.3 OCR 与 AI 抽取

- 前端调用 Supabase Edge Function `parse-receipt`。
- Edge Function 调用 Google Vision OCR。
- Edge Function 调用 OpenAI 抽取结构化 JSON。
- OpenAI 和 Google Vision Key 不进入前端。

抽取字段：

| 字段 | 说明 |
| --- | --- |
| `merchant_name` | 商户名称 |
| `company_reg_no` | 商业注册号 |
| `address` | 地址 |
| `phone` | 电话 |
| `invoice_no` | 发票/账单编号 |
| `date` | 日期，格式 `YYYY-MM-DD` |
| `time` | 时间，格式 `HH:MM` |
| `items` | 商品明细数组 |
| `subtotal` | 小计 |
| `discount` | 折扣 |
| `tax` | 税额 |
| `service_charge` | 服务费 |
| `rounding` | 舍入调整 |
| `grand_total` | 总金额 |
| `payment_method` | 支付方式 |
| `change` | 找零 |
| `subsidy_details` | 补贴信息 JSON |
| `category` | 行业分类 |
| `doc_type` | 单据类型 |
| `tags` | 标签数组 |
| `confidence_score` | 置信度 |

### 3.4 分类与标签

行业分类：

```text
Grocery / Fuel / F&B / Retail / Service / Other
```

单据类型：

```text
Receipt / Invoice / Credit Note / Expense
```

预设标签：

```text
Business / Personal / Tax Deductible / Pending
```

### 3.5 列表管理

列表支持：

- 搜索：商户名称、发票号。
- 筛选：状态、单据类型、标签、行业分类。
- 分页：默认每页 20 条。
- 状态显示：`uploaded`、`processing`、`pending_review`、`synced`、`failed`。
- 失败项可重试。

### 3.6 详情校对

详情面板包含：

- 原始收据预览。
- 商户信息编辑。
- 日期、发票号、支付方式编辑。
- 商品明细增删改。
- 金额校验：商品合计、折扣、税、服务费、舍入、总额。
- 标签编辑。
- 置信度提示。

### 3.7 Excel 导出

- 支持导出当前筛选结果。
- 支持导出已选记录。
- 商品明细按行展开，收据主信息重复填充。
- 前端使用 SheetJS 生成 `.xlsx`。

## 4. 非功能需求

| 类型 | 要求 |
| --- | --- |
| 安全 | 密钥只在 Edge Function Secrets；前端不持有 AI/OCR Key |
| 数据隔离 | Supabase RLS 强制按用户隔离 |
| 部署 | 前端静态部署，无自建服务器 |
| 性能 | 单张清晰收据 OCR+AI 目标 10 秒内 |
| 成本 | 主要成本来自 Google Vision 与 OpenAI 调用 |
| 可维护性 | 数据字段统一，不混用 `industry/tax_sst` 等旧字段 |

## 5. 技术架构

```text
用户浏览器
  -> Supabase Auth
  -> Supabase Storage
  -> Supabase Edge Function parse-receipt
      -> Google Vision
      -> OpenAI
      -> Supabase Postgres
  -> 前端列表/详情/导出
```

详细架构见 `docs/ARCHITECTURE.md`。

## 6. 验收标准

1. 用户可登录并上传收据。
2. 原始文件正确保存到 Supabase Storage。
3. Edge Function 可完成 OCR+AI 并写入数据库。
4. 列表可搜索、筛选、分页。
5. 详情可编辑主字段、商品明细、标签。
6. RLS 验证通过：用户不能访问他人数据。
7. Excel 导出字段完整，商品明细按行展开。
8. 50 张真实样本字段级准确率达到约定目标。

## 7. 已废弃范围

以下不再作为当前版本开发范围：

- Google Sheets 存储。
- Cloudflare Worker OCR/AI 代理。
- 无登录的匿名数据库写入。
- 前端直接调用 OpenAI 或 Google Vision。

